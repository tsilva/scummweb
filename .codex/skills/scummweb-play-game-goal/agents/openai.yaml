interface:
  display_name: "scummweb Play Game"
  short_description: "Achieve gameplay goals with minimal actions"
  default_prompt: "Use $scummweb-play-game-goal to achieve a specific in-game objective in scummweb with the fewest reliable actions: verify any seeded start state first, load local references before browsing, and stop to reclassify failures instead of brute-forcing interactions."
