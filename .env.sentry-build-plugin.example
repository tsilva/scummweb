# Sentry auth token for source map upload during production builds.
# Keep the real file in `.env.sentry-build-plugin` and do not commit it.
#
# Recommended scopes:
# - project:releases
# - org:read
SENTRY_AUTH_TOKEN=sntrys_your_token_here
